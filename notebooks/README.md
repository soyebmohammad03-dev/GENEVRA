# notebooks/

Exploratory analysis notebooks (results inspection, metric plots,
hypothesis exploration) over data produced by `experiments/` runs.
Notebooks are for exploration only — reusable logic that a notebook
discovers useful belongs in `src/genevra/`, not copy-pasted across
notebooks.

Empty for now: there are no results to explore yet.
