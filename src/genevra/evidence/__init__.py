"""Phase 19: the final research-evidence package.

Unlike every prior `genevra.artifacts`/`genevra.campaign` bundle (which
writes to the gitignored `research_artifacts/` scratch tree), this
package's output is meant to be *curated and committed* to
`research_evidence/` — a small, real, version-controlled sample of what
GENEVRA's machinery produces, not a second simulation engine. Every
number under `research_evidence/` traces to an actual run executed by
`genevra.evidence.build`.
"""
